import React from "react";
import {ListItemButton, ListItemText} from "@mui/material";


export const ListItem = () => {
    return (
        <ListItemButton>
            <ListItemText/>
        </ListItemButton>
    )
}